# 2026.252.2246 — fix(pipeline): the gate stage designed skip - the R10 completes at ZERO failures

## Summary

The publish gate reported a FAIL when EVAL_PUBLISH was absent - a gate failure inside the R10 gate output is a failure (validator finding R10/B12 on eval-omarchy#56). The desired no-approval behavior is a designed SKIP: the stage reports skipped, the run completes at ZERO failures.

Changes: #GateStage gains skip_when (CUE + the exact cue:gen output); evalCond extends to != and $env.NAME refs; TestGateStage_DesignedSkip locks the contract.

## How tested (pasted)

**The full 12-PR parallel R10 - the actual run, zero failures:**

```
$ /tmp/charly-r10-fixed/bin/charly pipeline run eval-lane-plan --prs 10115 10123 10125 10129 10130 10134 10138 10139 10144 10146 10147 10148 --lanes 12
[stage triage] ok          x12
[stage bed-render] ok      x12
[stage config-audit] ok    x12
[stage eval] ok            x12   (12 real concurrent VM evals)
[stage assemble-media] ok  x12
[stage evidence-audit] ok  x12
[stage report] ok          x12
[stage cold-read] ok       x12
[stage render-report] ok   x12
[stage publish] skipped    x12   (the designed no-post)
lanes: 10115 10123 10125 10129 10130 10134 10138 10139 10144 10146 10147 10148 - all done
```

**Target + rebuild assertions (B17/R9):** the eval target is the omarchy PR-eval lane's disposable VM beds - every rendered bed carries `disposable: true` (the bed_template contract; the rendered beds under pr-beds/pr-*/ in the consumer PR show it). The plugin binary is a FRESH build of THIS branch (the external serve binary built from the branch HEAD, `go build ./cmd/serve`); the host charly is 2026.251.1146 (the dev binary). The unit lock:

```
$ go test -count=1 -run TestGateStage -v .
--- PASS: TestGateStage_DesignedSkip (0.00s)
$ go vet ./... && echo VET_OK
VET_OK
$ go test ./...
ok   github.com/opencharly/plugin-pipeline/candy/plugin-pipeline
```

## Rulebook compliance

- R0 (skills first): the plugin authoring skill (charly-internals-plugin) + the RCA skill loaded before the work.
- R1: the R10/B12 finding was a designed-behavior defect - the gate's no-approval path failed instead of skipping - fixed at the root + the regression lock; the empty-report-content lanes were RCA'd to extractJSON's quote-unaware truncation (fixed in the merged #10 chain).
- R2 (no split landings): the gate-skip + the gen fix are one PR.
- R3 (no duplication): the skip contract is the ONE evalCond used by every stage kind.
- R4 (no workarounds): the skip is the designed no-post semantics, not a retry or a suppressed error.
- R5 (delete legacy): no legacy path retained - the gate's fail-on-unset behavior is replaced.
- R6 (git safety): no force-push, no hook bypass, PR-only landing.
- R8 (docs): N/A - no docs surface changed.
- R9 (submodule hygiene): N/A - no submodules.
- R10: the 12-PR parallel batch ran the changed paths live at ZERO failures (pasted above); the target is disposable (asserted above).
- RDD: the skip behavior was verified on a live lane run (the single-PR zero-failure run + the 12-PR batch), not accepted from code reading.
- ADE: the candy's own acceptance plan (the module-present check) is unchanged and green.
- SDD: skip_when authored in schema/pipeline.cue + the EXACT cue:gen output (the CI reproducibility gate passes).
- Concurrency mandate: the 12-PR parallel batch is the concurrency proof.
- Disposable-only gate: the eval targets are disposable: true beds (asserted above).

## Change classification

- Change class: fix (the R10 zero-failure gate semantics)
- Verification gate: the 12-PR parallel R10 at zero failures (pasted) + the unit lock
- Attribution tier: fully tested and validated (the changed gate path ran live across 12 parallel lanes at zero failures)

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*