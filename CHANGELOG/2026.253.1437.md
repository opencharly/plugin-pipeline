# 2026.253.1437 — feat(pipeline): the ledger-gate gains the corpus facts (corpus_run/corpus_ok/corpus_skipped)

## Summary

The Wave 6 lane-integration delta of the omarchy-test-corpus equivalence plan — FIVE files, all accounted:

1. **probes.go — the ledger-gate corpus facts**: corpus_run / corpus_ok / corpus_skipped — the omarchy-corpus surfaces of the eval bed's latest run, counted SEPARATELY from the oracle-authored assertions (never conflated — a corpus result is upstream's own contract). The identity source is the corpus charly.yml (the CUE-validated single source): every step `id:` in the omarchy-corpus candy's plan is a corpus step; the bed log's trailing `[id]` brackets are matched against it.
2. **probes.go — the ledger-gate key aliases**: the entity's input keys (bed_name/control_bed_name — the charly.yml contract) are honored; the key mismatch made the gate fail on the eval-omarchy lane (RCA'd live).
3. **executor.go — the trigger-aware FAIL-HARD**: the lane run exposed a REAL runner defect — the corpus-gate's redo trigger was UNREACHABLE (the FAIL-HARD check preempted it), so a probe fail ended the lane instead of redoing. The branch is now guarded (`err != nil && res.Trigger == ""`); a triggered fail restarts the chain from the target (bounded by the redo counters).
4. **corpus_facts_test.go + failhard_test.go + ledger_alias_test.go** — the unit locks.

## How tested (pasted)

**The fresh-rebuild + the version-match:**

```
$ go build -buildvcs=false -ldflags "-X main.BuildCalVer=2026.253.1550" -o /tmp/charly-stamped/bin/charly .
CHARLY BUILD-OK calver=2026.253.1550
$ /tmp/charly-stamped/bin/charly version
2026.253.1550
$ go build -o /tmp/fixed-plugins/plugin-pipeline ./cmd/serve
PIPELINE BUILD-OK
```

**The unit suite:**

```
$ go test -count=1 -run "TestCorpusFacts|TestTriggeredFailIsRedoNotFailHard|TestProbeLedgerGate_Aliases" -v .
--- PASS: TestCorpusFacts (0.00s)
--- PASS: TestTriggeredFailIsRedoNotFailHard (0.00s)
--- PASS: TestProbeLedgerGate_Aliases (0.00s)
$ go test -count=1 ./...
ok  	github.com/opencharly/plugin-pipeline/candy/plugin-pipeline	0.004s
$ go vet ./... && gofmt -l .
VET-OK
FMT-CLEAN
```

**The live lane run (pr-10115, the STAMPED charly + the freshly-built pipeline plugin, the fresh golden):**

```
[stage corpus-gate] fail — corpus_audit: the selected corpus step 'accept-shell' is not in the omarchy-corpus plan (charly.yml) - the oracle's gap
[agent triage] response: The corpus-gate rejected `accept-shell` too ... the corpus must be empty with an honest note.
[stage corpus-gate] ok
[stage control] ok — check-run exit 0 (the negative control passed)
[stage eval] ok — check-run exit 2 (the PR's own test fails on the applied system — a REAL finding)
[stage ledger-gate] ok
lane 10115: done
```

**The ledger facts (stage-findings.yml) — the corpus facts computed live:**

```
- stage: ledger-gate
  outputs: {"control_ok":true,"corpus_ok":25,"corpus_run":25,"corpus_skipped":0,"executed_checks":34,"media_ok":true}
```

All 25 corpus surfaces ran and passed in the PR eval bed; the changed paths (the corpus facts, the trigger-aware redo, the key aliases) executed live against the fresh rebuild.

## Rulebook compliance

- R0: the plugin skill loaded; the probe pattern followed.
- R1: the corpus results were invisible in the ledger; the redo trigger was unreachable (both RCA'd live and fixed at the root).
- R3: the corpus identity comes from the charly.yml single source (no side file).
- R4: no workarounds.
- R6: no force-push, no hook bypass, PR-only landing.
- R9: the deployed binary rebuilt with `charly version` matching source (pasted).
- R10: the fresh-rebuild live lane run + the unit tests pasted.

## Change classification

- Change class: feat (the ledger-gate corpus facts) + fix (the trigger-aware FAIL-HARD + the key aliases)
- Verification gate: the fresh-rebuild live lane run + the unit tests (pasted)
- Attribution tier: fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*