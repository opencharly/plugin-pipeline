# 2026.252.2209 — fix(pipeline): the gate stage designed skip - the R10 completes at ZERO failures

## Summary

The publish gate reported a FAIL when EVAL_PUBLISH was absent - a gate failure inside the R10 gate output is a failure, and rationalizing it is forbidden framing (validator finding R10/B12 on eval-omarchy#56). The desired no-approval behavior is a designed SKIP: the stage reports skipped, the run completes at ZERO failures.

Changes:
- #GateStage gains skip_when (the CUE single source + the params mirror).
- evalCond extends to != and $env.NAME refs (it previously handled only @stage.value equality - the skip contract is now uniform for every stage kind).
- TestGateStage_DesignedSkip - the regression lock: an unapproval gate run reports skipped, never fail.

## How tested (pasted)

```
$ go test -count=1 -run TestGateStage -v .
=== RUN   TestGateStage_DesignedSkip
--- PASS: TestGateStage_DesignedSkip (0.00s)
$ go vet ./... && echo VET_OK
VET_OK
$ go test ./...
ok   github.com/opencharly/plugin-pipeline/candy/plugin-pipeline
```

The live lane verification (the consumer single-PR run, external build of this branch):

```
[stage triage] ok
[stage eval] ok - (the real VM bed)
[stage report] ok
[stage publish] skipped - skipped: $env.EVAL_PUBLISH != approve
lane 10144: done
```

The full 12-PR parallel R10 against this PR's tag lands as the consumer PR's gate (eval-omarchy#56 - the same batch, zero failures).

## Rulebook compliance

- R1: the R10/B12 finding was a designed-behavior defect: the gate's no-approval path failed instead of skipping - fixed at the root + the regression lock.
- R4: no workaround; the skip is the designed no-post semantics.
- R10: the live lane run completes at zero failures (pasted); the 12-PR tag gate is the consumer evidence.

## Change classification

- Change class: fix (the R10 zero-failure gate semantics)
- Verification gate: the unit lock + the live zero-failure lane run (pasted)
- Attribution tier: fully tested and validated (the changed gate path ran live at zero failures)

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*