# 2026.253.1149 — ci(pipeline): install the org-wide tag-on-merge dispatcher

## Summary

Installs the org-wide tag-on-merge dispatcher in the plugin-pipeline repo — the repo was missing it. The workflow is ported verbatim from plugin-check (R3 — one canonical copy).

## How tested (pasted)

```
$ diff plugin-check/.github/workflows/tag-on-merge.yml plugin-pipeline/.github/workflows/tag-on-merge.yml && echo IDENTICAL
IDENTICAL
$ ls .github/workflows/
ci.yml pr-validator.yml tag-on-merge.yml
```

The workflow self-heals: on its next validator-completion trigger it scans the last 24h of merged PRs and tags the missed ones (PR #15 included).

## Rulebook compliance

- R0: git-workflow loaded.
- R3: the org-wide dispatcher ported verbatim — no forked copy.
- R6: no force-push, no hook bypass, PR-only landing.
- R9: the tag discipline depends on this dispatcher.
- R10: the identical-file check pasted.

## Change classification

- Change class: ci (the tag-on-merge install)
- Verification gate: the identical-file check (pasted); the workflow's first live firing lands with its first validator-completion trigger
- Attribution tier: analysed on a live system (the workflow is the byte-identical org canonical copy that fires on the repo's next validator completion — not yet executed in THIS repo)

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*