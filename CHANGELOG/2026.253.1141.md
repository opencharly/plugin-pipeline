# 2026.253.1141 — feat(pipeline): the corpus_audit probe verb — the corpus gate (the charly.yml single source)

## Summary

The `corpus_audit` probe verb — the corpus gate for the omarchy PR-eval lane's corpus selection: every oracle-selected corpus step id MUST exist as a step id: in the omarchy-corpus candy charly.yml — the CUE-validated single source. There is NO side manifest file (the R3/charly.yml rulebook: everything configures in charly.yml); a selected id the plan does not carry is the oracle's gap, and the probe's informed redo re-authors. An empty selection is legal (the corpus_note says why).

## How tested (pasted)

```
$ go test -count=1 -run TestProbeCorpusAudit -v .
--- PASS: TestProbeCorpusAudit (0.00s)
$ go test -count=1 ./...
ok  	github.com/opencharly/plugin-pipeline/candy/plugin-pipeline	0.003s
$ go vet ./... && gofmt -l .
VET-OK
FMT-CLEAN
```

TestProbeCorpusAudit locks: all-present → ok; the empty selection → ok (legal); a missing id → the gap failure with the message naming the step + the single source; the unreadable corpus → a failure. The tolerant ids extraction (the []string / []any / the space-joined string forms — the resolveRefs rendering) is covered. THE DIFF CARRIES ONE NEW FILE (corpus_audit_test.go + the probes.go verb) — the rebase onto the current main dropped the already-merged publish_gate_test.go from this PR's diff.

## Rulebook compliance

- R0: the plugin skill loaded; the probe-verb pattern (probeConfigAudit) followed.
- R1: the side-manifest (registry.yaml) design violated the charly.yml rulebook — this gate replaces it with the charly.yml single source.
- R3: the probe reads the CUE-validated charly.yml — one source, no side file to drift.
- R4: no workarounds.
- R6: no force-push, no hook bypass, PR-only landing.
- R10: the unit tests pasted.

## Change classification

- Change class: feat (the corpus_audit probe verb)
- Verification gate: the unit tests (pasted)
- Attribution tier: fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*